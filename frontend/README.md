# Minitasks Frontend

This is a Vite + React frontend using TailwindCSS.

Setup

```powershell
cd frontend
npm install
npm run dev
```

By default the frontend expects the backend API at `http://localhost:5000/api`. To change, set `VITE_API_URL` in your environment.
